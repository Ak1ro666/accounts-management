import { createTheme } from "@mui/material";

export const themeConfig = createTheme({
  palette: {
    primary: {
      // main: "#ca0000",
      main: "#2962ff",
    },
    secondary: {
      // main: "#2e4c9b",
      main: "#2962ff",
    },
  },
});
